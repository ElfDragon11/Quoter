package com.example.quoter.ui.theme

import androidx.compose.ui.graphics.Color

// Define new Blue Palette based on #0F3554
val PrimaryBlue = Color(0xFF0F3554)      // Base color
val PrimaryBlueLight = Color(0xFF164A75) // Lighter shade
val PrimaryBlueDark = Color(0xFF082033)  // Darker shade

// New Dark Theme Palette (Based on example images)
val DarkBackground = Color(0xFF121212) // Very dark grey
val DarkSurface = Color(0xFF1E1E1E) // Slightly lighter dark grey for surfaces
val MutedCyan = Color(0xFF00838F) // Less bright Cyan/Teal
val OnDarkColor = Color(0xFFE0E0E0) // Light grey for text/icons on dark backgrounds
val OnPrimaryColor = Color(0xFFFFFFFF) // White for text/icons on primary color (MutedCyan)